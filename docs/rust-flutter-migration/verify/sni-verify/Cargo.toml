[package]
name = "sni-verify"
version = "0.1.0"
edition = "2021"

[dependencies]
rustls = { version = "=0.23.43", default-features = false, features = ["ring", "std", "tls12"] }
rustls-pki-types = "1"
webpki-root-certs = "1.0"
