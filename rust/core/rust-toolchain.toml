[toolchain]
# 固定具体版本。构建钩子不接受浮动的 stable / beta / nightly 通道，
# 否则同一份代码在不同时间会解析到不同工具链。
channel = "1.96.0"
# flutter build macos 按通用二进制构建，两种架构都会请求一次构建钩子，
# 因此两个目标都必须列出，缺一个会直接报 RustValidationException。
targets = ["aarch64-apple-darwin", "x86_64-apple-darwin"]
